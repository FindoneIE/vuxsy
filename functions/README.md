# Findone Cloud Functions

This folder hosts Firebase Cloud Functions for background jobs such as image optimization.

## Quick start

- Install dependencies in `functions/`.
- Build with `npm run build`.
- Deploy with `npm run deploy`.

The image optimization triggers listen for uploads under `tmp/listings/**` and `tmp/avatars/**`.
